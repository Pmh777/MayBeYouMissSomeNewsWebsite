@using MayBeYouMissSomeNews.Models
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        @if (Session["Account"] != null)
        {

            user u = (user)Session["Account"];
            <li>hi! @u.name </li>}
        @if (Session["AccountAdmin"] != null)
        {

            employee u = (employee)Session["AccountAdmin"];
            <li>Chào Bạn! @u.name </li>}
    </div>
</body>
</html>
