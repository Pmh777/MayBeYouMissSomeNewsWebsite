@model MayBeYouMissSomeNews.Models.category

@{
    ViewBag.Title = "CreateCategory";
    Layout = "~/Views/Shared/LayoutOfAdmin.cshtml";
}

<h2>CreateCategory</h2>

@using (Html.BeginForm("CreateCategory", "Categories", FormMethod.Post, new { @role = "form" }))
{
    @Html.AntiForgeryToken()

    @* <form role="form">*@
    <div class="form-group mb-3">
        <div class="input-group input-group-merge input-group-alternative">
            <div class="input-group-prepend">
                <span class="input-group-text"><i class="fa fa-user-o"></i></span>
            </div>
            @*<input class="form-control" placeholder="Email" type="email">*@
            @Html.TextBox("name", "", new { required = "required", placeholder = "Name", type = "text", @class = "form-control" })
        </div>
    </div>
    <div class="form-group mb-3">
        <div class="input-group input-group-merge input-group-alternative">
            <div class="input-group-prepend">
                <span class="input-group-text"><i class="line-numbers-rows" ></i></span>
            </div>
            @*<input class="form-control" placeholder="Email" type="email">*@
            @Html.TextBox("status", "", new { required = "required", placeholder = "Status", type = "number", @class = "form-control" })
        </div>
    </div>
    @*<div class="form-group mb-3">
        <div class="input-group input-group-merge input-group-alternative">
            <div class="input-group-prepend">
                <span class="input-group-text"><i class="line-numbers-rows("1,2,3")"></i></span>
            </div>
            @*<input class="form-control" placeholder="Email" type="email">
            @Html.TextBox("createdby", "", new { required = "required", placeholder = "Createdby", type = "number", @class = "form-control" })
        </div>
    </div>
        *@
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>
}

